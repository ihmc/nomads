NetSensor instruction

1) Unpack the whole archive in <folder>

2) Go in <folder>\misc\scripts\netSensor

3) Run sudo sh installDependencies.sh (it will take a while...)

4) Run sh configureNetSensor.sh (generally you only want to touch IP, PORT and INTERFACE)

5) Run sudo sh launchNetSensor.sh